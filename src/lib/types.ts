export type RequestedConsultationsProps = {
  id: number;
  customer_id: number;
  name: string;
  topic: string;
  hopeDay: string;
  requestDay: string;
  approvalStatus: string;
  finishStatus: boolean;
}